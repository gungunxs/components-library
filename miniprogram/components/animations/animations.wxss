/* 淡入动画 */
@keyframes fadeIn {
	0% { opacity: 0 }
	100% { opacity: 1 }
}
/* 淡出动画 */
@keyframes fadeOut {
	0% { opacity: 1 }
	100% { opacity: 0 }
}

/* 渐显放大动画 */
@keyframes fadeInGrow {
  0% {
    transform: scale(0.5);
    opacity: 0;
  }
  100% {
    transform: scale(1);
    opacity: 1;
  }
}

/* 渐隐缩小动画 */
@keyframes fadeOutShrink {
  0% {
    transform: scale(1);
    opacity: 1;
  }
  100% {
    transform: scale(0.5);
    opacity: 0;
  }
}


